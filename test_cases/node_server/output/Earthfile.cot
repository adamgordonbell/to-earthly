Here's the Earthfile converted from the multistage Dockerfile:

```
VERSION 0.7

all:
  BUILD +final

base:
  FROM node:19-alpine3.16
  WORKDIR /app
  COPY package.json ./
  RUN npm install
  COPY src/ ./src
  COPY quotes.txt ./quotes.txt

test:
  FROM +base
  RUN npm test

final:
  FROM +base
  EXPOSE 8003
  ENTRYPOINT [ "node", "src/index.js" ]
  SAVE IMAGE --push $DOCKERHUB_USERNAME/earthly-node-example:gh-actions-only
```