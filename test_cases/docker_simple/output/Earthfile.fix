VERSION 0.7

all:
  BUILD +docker

docker:
  FROM nginx:latest
  WORKDIR /usr/share/nginx/html
  COPY ./readme.txt .
  EXPOSE 80
  CMD ["nginx", "-g", "daemon off;"]
  ARG TIMESTAMP
  SAVE IMAGE --push my-image-name:$TIMESTAMP

# Usage: earthly --build-arg TIMESTAMP=$(date +%s) +all